pipeline {

  agent any

  environment {
    SVC_ACCOUNT_KEY = credentials('terraform-auth')
  }

  stages {

    stage('Checkout') {
      steps {
        checkout scm
        sh 'mkdir -p creds' 
        sh 'echo $SVC_ACCOUNT_KEY | base64 -d > ./creds/serviceaccount.json'
      }
    }

    stage('TF init') {
      steps {
        container('terraform') {
          sh 'terraform init'
          sh 'echo "You will destory the following if you confirm"'
          sh 'terraform state list'
          sh 'echo "graph"'
          sh 'terraform graph'
          sh 'terraform show'
        }
      }      
    }

     
    stage('Approval') {
      steps {
        script {
          def userInput = input(id: 'confirm', message: 'Apply Terraform destroy?', parameters: [ [$class: 'BooleanParameterDefinition', defaultValue: false, description: 'Apply terraform', name: 'confirm'] ])
        }
      }
    }
    stage('TF destroy') {
      steps {
        container('terraform') {
          sh 'terraform destroy --auto-approve'
          
        }
      }      
    }

    

   
  } 

}
